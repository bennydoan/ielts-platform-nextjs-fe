import { AuthIndex } from "@/components";
import { RegisterForm } from "@/components";

function Register() {
  return (
    <AuthIndex>
      <RegisterForm />
    </AuthIndex>
  );
}

export default Register;
